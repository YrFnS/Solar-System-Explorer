# Track large 3D model files with Git LFS
*.glb filter=lfs diff=lfs merge=lfs -text
*.gltf filter=lfs diff=lfs merge=lfs -text

# KTX2 files are committed directly so deployments receive real binary payloads.
*.ktx2 binary -diff -merge
